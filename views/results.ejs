<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Available Rooms</title>
    <style>
      body {
        font-family: sans-serif;
        max-width: 800px;
        margin: 2rem auto;
        padding: 1rem;
      }
      h1 {
        color: #333;
      }
      a {
        text-decoration: none;
        color: #007bff;
      }
      ul {
        list-style-type: none;
        padding: 0;
      }
      li {
        background-color: #f9f9f9;
        border: 1px solid #ddd;
        padding: 1rem;
        margin-bottom: 0.5rem;
        border-radius: 5px;
      }
      .building-group {
        border: 1px solid #ccc;
        border-radius: 8px;
        margin-bottom: 1rem;
        overflow: hidden;
      }
      .building-group[open] {
        border-color: #007bff;
      }
      .building-name {
        font-size: 1.1rem;
        font-weight: bold;
        padding: 1rem;
        background-color: #f0f0f0;
        cursor: pointer;
        display: flex;
        align-items: center;
        gap: 0.5rem;
        flex-wrap: wrap;
      }
      .building-name:hover {
        background-color: #e4e4e4;
      }
      .room-count {
        font-weight: normal;
        color: #555;
        font-size: 0.9rem;
      }
      .building-distance {
        font-weight: normal;
        color: #007bff;
        font-size: 0.85rem;
        margin-left: auto;
      }
      .building-group ul {
        padding: 0.5rem 1rem 0.5rem 1rem;
      }
      .room-name {
        font-size: 1.1rem;
        font-weight: bold;
      }
      .details {
        color: #555;
      }
    </style>
  </head>
  <body>
    <h1>Best Available Rooms for You</h1>

    <% if (rooms.length > 0) { %>
    <p>Found <%= rooms.length %> empty room(s) for the <%= timeSlot.start %> - <%= timeSlot.end %> slot, grouped by building.</p>

    <% // Group rooms by building const grouped = {}; rooms.forEach(room => { if (!grouped[room.building]) { grouped[room.building] = { rooms: [],
    minDistance: Infinity, lat: room.lat, lon: room.lon }; } grouped[room.building].rooms.push(room); if (room.totalDistance <
    grouped[room.building].minDistance) { grouped[room.building].minDistance = room.totalDistance; } }); // Sort buildings by closest room distance
    const sortedBuildings = Object.keys(grouped).sort( (a, b) => grouped[a].minDistance - grouped[b].minDistance ); %> <%
    sortedBuildings.forEach(building => { %>
    <details class="building-group">
      <summary class="building-name">
        <%= building %>
        <span class="room-count">(<%= grouped[building].rooms.length %> room<%= grouped[building].rooms.length !== 1 ? 's' : '' %>)</span>
        <span class="building-distance">~<%= (grouped[building].minDistance * 1000).toFixed(0) %>m away</span>
      </summary>
      <ul>
        <% grouped[building].rooms.forEach(room => { %>
        <li>
          <div class="room-name"><%= room.name %></div>
          <div class="google-maps">
            <a href="https://www.google.com/maps/search/<%= room.lat %>,<%= room.lon %>" target="_blank">View on Google Maps</a>
          </div>
          <div class="details">
            Total Walk:
            <strong
              ><%= (room.totalDistance * 1000).toFixed(0) %>
              meters</strong
            >
          </div>
        </li>
        <% }); %>
      </ul>
    </details>
    <% }); %>

    <% } else { %>
    <p>Sorry, no suitable empty rooms were found for the <%= timeSlot.start %> - <%= timeSlot.end %> slot. 😞</p>
    <% } %>

    <a href="/">&#8592; Try Again</a>
  </body>
</html>
